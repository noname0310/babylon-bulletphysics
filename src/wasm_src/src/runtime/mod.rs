pub(crate) mod collision_shape;

use wasm_bindgen::prelude::*;

#[wasm_bindgen]
pub struct Runtime {
        
}

impl Runtime {
    pub fn new() -> Self {
        Self {
            
        }
    }
}
